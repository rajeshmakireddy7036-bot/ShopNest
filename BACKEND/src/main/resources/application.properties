
# Application Name
spring.application.name=ShopNest-Backend

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://rajeshmakireddy7036_db_user:Rajesh7036@cluster0.udtthjg.mongodb.net/shopnest_db
# The database will be created as "shopnest_db" if it doesn't exist

# Server Port (changing default to 8080 or user's preference)
server.port=8081

# Logging
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.springframework.web=INFO
logging.level.com.shopnest.backend=DEBUG

# Spring JPA / Security (optional for now but good to prepare later)
# spring.main.allow-bean-definition-overriding=true

# DevTools
spring.devtools.livereload.enabled=true
